```
pip install uv
```
this cmd especially used to install uv or any other package in python
  ``` 
  uv add fastapi[all] langchain langchain-openai python-dotenv sqlalchemy uvicorn  psycopg2-binary 
```
